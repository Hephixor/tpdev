<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/action_settings"
        android:orderInCategory="100"
        android:title="Settings"
        app:srcCompat="@drawable/ic_alarm_white"
        android:tint="@color/colorAccent"
        android:iconTint="@color/new_event_hint_text_color"
        app:showAsAction="always" />

    <item android:id="@+id/action_today"
        android:title="@string/action_today"
        android:orderInCategory="100"
        app:showAsAction="always"/>



        <group android:checkableBehavior="single">

            <item android:id="@+id/action_day_view"
                android:title="@string/action_day_view"
                android:orderInCategory="100"
                app:showAsAction="ifRoom"/>

            <item android:id="@+id/action_three_day_view"
                android:title="@string/action_three_day_view"
                android:orderInCategory="100"
                app:showAsAction="ifRoom"
                android:checked="true"/>

            <item android:id="@+id/action_week_view"
                android:title="@string/action_week_view"
                android:orderInCategory="100"
                app:showAsAction="ifRoom"/>
        </group>
</menu>
